# vhc_simplifier requirements
# Requires Python 3.12+ (tested on 3.12 and 3.13)

# Core
pandas>=2.0.0

# Optional integrations (install only what you need)
simple-salesforce>=1.12.0   # --sf-account-id: Salesforce Task creation
httpx>=0.27.0               # Slack webhook (stdlib urllib used as fallback if absent)

# Dev / testing (run: python -m pytest tests/ --cov=vhc_simplifier)
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.15.0
